# --- Dependencias: se regeneran con npm install, JAMÁS al repo ---
/node_modules

# --- Build de Next.js: se regenera con npm run build ---
/.next/
/out/
/build

# --- Secretos: NUNCA al repo (es seguridad, no estilo) ---
.env
.env*.local

# --- Vercel ---
.vercel

# --- TypeScript (archivos generados) ---
*.tsbuildinfo
next-env.d.ts

# --- Logs y sistema ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.pem
